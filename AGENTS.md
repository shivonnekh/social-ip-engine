# AGENTS.md

**The instructions for this repo live in [`CLAUDE.md`](./CLAUDE.md). Read that.**
For the `studio/` content factory, read [`studio/CLAUDE.md`](./studio/CLAUDE.md).

This file is a pointer on purpose, and used to be a problem.

It was a 554-line copy of `CLAUDE.md` that differed in exactly 10 lines —
all of them rewriting paths to `/Users/shivonne/Codex/social-ip-engine`, a
directory that does not exist on this machine. So the duplicate was not just
redundant, it was already wrong, and every future edit to `CLAUDE.md` would
have widened the gap silently. Two 554-line files that must agree by hand
will not stay in agreement.

Anything that reads `AGENTS.md` should follow the link. Do not paste the
contents of `CLAUDE.md` back into this file.
